#container
  - 50.times do
    %h1.item
      = @arr.sample
    - 3.times do
      %h2.item
        = @arr.sample
    - 5.times do
      %h3.item
        = @arr.sample
.container
  %h1#title
    = @arr.join

